Continuation from the previous task. Create window event listener for double click that makes the user enter and exit fullscreen. RECOMMENDATION: Instead of continuing from the previous tasks, rewrite the whole code again, to memorize the syntax.

