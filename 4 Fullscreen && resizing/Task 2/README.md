Continuation from the previous task.
Listen to the resize event and handle resizing. (Hint: Use 'resize' window event listener, camera.aspect, camera.updateProjectionMatrix(), renderer.setSize) 
RECOMMENDATION: Instead of continuing from the previous tasks, rewrite the whole code again, to memorize the syntax.

