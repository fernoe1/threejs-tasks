Continuation from the previous task. Make it so the positions for the vertices are randomly generated, you can set the amount of vertices to generate (count) yourself. (Hint: use Math.random()) RECOMMENDATION: Instead of continuing from the previous tasks, rewrite the whole code again, to memorize the syntax.

