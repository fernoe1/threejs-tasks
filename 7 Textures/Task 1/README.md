Create a cube with orbit controls, and apply a texture to it using MeshBasicMaterial with the map property. Load the texture from "textures/door/color.jpg" using the image onload method.

You must have CSS that makes your canvas take whole screen.



You must have resize event listener that listens to resizes.

