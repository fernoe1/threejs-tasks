Create a cube. Enable orbit controls. Create LoadingManager to handle global loading state. Initialize TextureLoader with LoadingManager, load all the textures in textures directory. When using LoadingManager, log onStart, onLoad, onProgress, onError states using console.log(). Apply a texture of your choice to the cube.



You must have CSS that makes your canvas take whole screen.



You must have resize event listener that listens to resizes.



RECOMMENDATION: Instead of continuing from the previous tasks, rewrite the whole code again, to memorize the syntax.

