Continuation from the previous task. Handle renderer pixel ratio. Make it so renderer cannot exceed device pixel ratio and pixel ratio of 2. (Hint: Use window.devicePixelRatio)
RECOMMENDATION: Instead of continuing from the previous tasks, rewrite the whole code again, to memorize the syntax.

